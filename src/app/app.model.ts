

export interface postInterface {
    id : number,
    title : string,
    content : string,
    page : number
  }

  
export interface addPostInterface
{
  postTitle : string,
  postContent : string,
  postPage : number
}